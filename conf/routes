# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET /walls.json           controllers.WallController.listAllWalls()

GET /walls.json        controllers.WallController.listWallsForLocation(startLatitude: Double, startLongitude: Double, stopLatitude: Double, stopLongitude: Double, )

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
